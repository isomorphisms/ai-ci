{
  "name": "ai-ci",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Fail-closed acceptance contracts for isomorphisms projects",
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
